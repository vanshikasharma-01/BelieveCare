{
  "name": "believecare-pharmacy-app",
  "version": "1.0.0",
  "private": true,
  "description": "Root scripts to run the Believecare backend and frontend together with a single command.",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix frontend",
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run server\" \"npm run client\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "all": "^0.0.0"
  }
}
